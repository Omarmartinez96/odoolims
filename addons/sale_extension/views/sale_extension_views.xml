<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Heredar la vista de lista para agregar el campo "client_code" -->
    <record id="view_partner_tree_inherited" model="ir.ui.view">
        <field name="name">res.partner.tree.inherited</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//list/field[@name='complete_name']" position="after">
                <field name="client_code" string="Código de Cliente" optional="show"/>
            </xpath>
        </field>
    </record>

    <!-- Heredar la vista de formulario para agregar "client_code" -->
    <record id="view_partner_form_inherited" model="ir.ui.view">
        <field name="name">res.partner.form.inherited</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='vat']" position="after">
                <field name="client_code" string="Código de Cliente"/>
            </xpath>
        </field>
    </record>
</odoo>
