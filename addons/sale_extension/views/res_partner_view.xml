<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_partner_form_inherit" model="ir.ui.view">
        <field name="name">res.partner.form.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Sucursales">
                    <field name="sucursales_ids" widget="one2many_list" editable="bottom">
                        <list>
                            <field name="name" string="Identificación"/>
                            <field name="direccion" string="Dirección"/>
                            <field name="observaciones" string="Observaciones"/>
                        </list>
                    </field>
                </page>
            </notebook>
        </field>
    </record>
</odoo>
