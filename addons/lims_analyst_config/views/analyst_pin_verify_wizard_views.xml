<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        
        <!-- Wizard para verificar PIN -->
        <record id="view_analyst_pin_verify_wizard_form" model="ir.ui.view">
            <field name="name">analyst.pin.verify.wizard.form</field>
            <field name="model">analyst.pin.verify.wizard</field>
            <field name="arch" type="xml">
                <form string="Verificar PIN del Analista">
                    <div class="alert alert-info" role="alert">
                        <strong>🔐 Verificación de Identidad</strong><br/>
                        Confirme su identidad ingresando su PIN personal.
                    </div>
                    
                    <group>
                        <field name="analyst_id" readonly="1"/>
                        <field name="pin_input" password="True" 
                               placeholder="Ingrese su PIN de 4-6 dígitos"
                               autofocus="1"/>
                    </group>
                    
                    <footer>
                        <button name="verify_pin" type="object" 
                                string="✅ Verificar PIN" 
                                class="btn-success"/>
                        <button string="❌ Cancelar" 
                                class="btn-secondary" 
                                special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

    </data>
</odoo>