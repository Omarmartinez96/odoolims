<odoo>
    <data>

        <!-- Menú Principal de Gestión de Clientes -->
        <menuitem id="lims_customer_menu" name="Gestión de Clientes" sequence="1"/>

        <!-- Acción para Clientes -->
        <record id="action_lims_customers" model="ir.actions.act_window">
            <field name="name">Clientes</field>
            <field name="res_model">lims.customer</field>
            <field name="view_mode">list,form</field>
        </record>

        <!-- Submenú Clientes -->
        <menuitem id="lims_customers_menu" name="Clientes" parent="lims_customer_menu" action="action_lims_customers" sequence="2"/>

        <!-- Vista de Lista para Clientes -->
        <record id="view_lims_customer_list" model="ir.ui.view">
            <field name="name">lims.customer.list</field>
            <field name="model">lims.customer</field>
            <field name="arch" type="xml">
                <list string="Clientes">
                    <field name="name"/>
                    <field name="client_code"/>
                    <field name="fiscal_address"/>
                </list>
            </field>
        </record>

        <!-- Vista de Formulario para Clientes -->
        <record id="view_lims_customer_form" model="ir.ui.view">
            <field name="name">lims.customer.form</field>
            <field name="model">lims.customer</field>
            <field name="arch" type="xml">
                <form string="Gestión de Clientes">
                    <sheet>
                        <group>
                            <field name="name" required="1"/>
                            <field name="client_code"/>
                            <field name="fiscal_address"/>
                        </group>

                        <notebook>
                            <!-- Pestaña Sucursales -->
                            <page string="Sucursales">
                                <field name="branch_ids" context="{'default_customer_id': id}">
                                    <list editable="bottom">
                                        <field name="name"/>
                                        <field name="address"/>
                                    </list>
                                    <form>
                                        <sheet>
                                            <group>
                                                <field name="name"/>
                                                <field name="address"/>
                                                <field name="customer_id" invisible="1"/>
                                            </group>
                                        </sheet>
                                    </form>
                                </field>
                            </page>
                        </notebook>

                    </sheet>
                </form>
            </field>
        </record>

        <!-- Vista de Formulario para Sucursales -->
        <record id="view_lims_branch_form" model="ir.ui.view">
            <field name="name">lims.branch.form</field>
            <field name="model">lims.branch</field>
            <field name="arch" type="xml">
                <form string="Sucursal">
                    <sheet>
                        <group>
                            <field name="name" required="1"/>
                            <field name="address"/>
                            <field name="customer_id"/>
                        </group>

                        <notebook>
                            <page string="Departamentos">
                                <field name="department_ids">
                                    <list editable="bottom">
                                        <field name="name"/>
                                    </list>
                                    <form>
                                        <sheet>
                                            <group>
                                                <field name="name"/>
                                                <field name="branch_id" invisible="1"/>
                                            </group>
                                        </sheet>
                                    </form>
                                </field>
                            </page>
                        </notebook>

                    </sheet>
                </form>
            </field>
        </record>

    </data>
</odoo>
