<odoo>
    <data>
        <!-- Vista de Lista -->
        <record id="view_lims_customer_list" model="ir.ui.view">
            <field name="name">lims.customer.list</field>
            <field name="model">lims.customer</field>
            <field name="arch" type="xml">
                <list string="Clientes">
                    <field name="name"/>
                    <!-- <field name="rfc"/>  ⚠️ Eliminar o agregar si existe -->
                    <field name="billing_partner_id"/>
                </list>
            </field>
        </record>

        <!-- Vista de Formulario -->
        <record id="view_lims_customer_form" model="ir.ui.view">
            <field name="name">lims.customer.form</field>
            <field name="model">lims.customer</field>
            <field name="arch" type="xml">
                <form string="Cliente">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <!-- <field name="rfc"/> ⚠️ Eliminar si no está en el modelo -->
                            <field name="billing_partner_id"/>
                        </group>
                        <notebook>
                            <page string="Sucursales">
                                <field name="branch_ids">
                                    <list editable="bottom">
                                        <field name="name"/>
                                        <field name="address"/>
                                    </list>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Acción -->
        <record id="action_lims_customers" model="ir.actions.act_window">
            <field name="name">Clientes</field>
            <field name="res_model">lims.customer</field>
            <field name="view_mode">list,form</field>
        </record>

        <!-- Menú -->
        <menuitem id="lims_menu" name="LIMS" sequence="1"/>
        <menuitem id="lims_customers_menu" name="Clientes"
