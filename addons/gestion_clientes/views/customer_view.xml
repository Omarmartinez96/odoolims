<odoo>
    <!-- Form View for Customer Management -->
    <record id="view_customer_form" model="ir.ui.view">
        <field name="name">customer.management.form</field>
        <field name="model">customer.management</field>
        <field name="arch" type="xml">
            <form string="Gestión de Clientes">
                <sheet>
                    <group>
                        <field name="name" required="1" string="Razón Social"/>
                        <field name="rfc" string="RFC"/>
                        <field name="fiscal_address" string="Dirección Fiscal"/>
                        <field name="client_code" string="Código de Cliente"/>
                        <field name="fiscal_certificate" string="Constancia de Situación Fiscal" filename="certificate_filename"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- List View for Customer Management -->
    <record id="view_customer_list" model="ir.ui.view">
        <field name="name">customer.management.list</field>
        <field name="model">customer.management</field>
        <field name="arch" type="xml">
            <list string="Clientes">
                <field name="name" string="Razón Social"/>
                <field name="rfc" string="RFC"/>
                <field name="fiscal_address" string="Dirección Fiscal"/>
                <field name="client_code" string="Código de Cliente"/>
            </list>
        </field>
    </record>

    <!-- Action for the Customer Management model -->
    <record id="action_customer_management" model="ir.actions.act_window">
        <field name="name">Gestión de Clientes</field>
        <field name="res_model">customer.management</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Crea un nuevo cliente
            </p>
        </field>
    </record>

    <!-- Menu item for accessing the Customer Management list -->
    <menuitem id="menu_customer_management" name="Gestión de Clientes" action="action_customer_management"/>
</odoo>
