<odoo>
  <template id="inherit_account_report_invoice_document"
            inherit_id="account.report_invoice_document"
            priority="30">

    <!-- capturamos cualquier layout estándar -->
    <xpath expr="//t[contains(@t-call, 'external_layout')]" position="replace">

      <t t-call="lims_accounting_extension.external_layout_invoice_lims">
        <!-- variable que usa tu header -->
        <t t-set="company" t-value="docs and docs[0].company_id"/>

        <!-- SLOT de contenido: solo el cuerpo, nada de layouts -->
        <t t-slot="content">
          <!-- reutilizamos el cuerpo estándar -->
          <t t-call="account.report_invoice_document_content"/>
          <!-- si tu instalación no tiene ese template, pega aquí las líneas/totales -->
        </t>
      </t>

    </xpath>
  </template>
</odoo>
