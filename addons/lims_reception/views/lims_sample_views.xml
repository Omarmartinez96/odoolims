<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Vista de Formulario para Recepción de Muestras -->
        <record id="view_lims_sample_form" model="ir.ui.view">
            <field name="name">lims.sample.form</field>
            <field name="model">lims.sample</field>
            <field name="arch" type="xml">
                <form string="Recepción de Muestra">
                    <sheet>
                        <group>
                            <field name="cliente_id" required="1"/>
                            <field name="billing_partner_id" readonly="1"/>
                            <field name="sucursal_id" required="1"/>
                            <field name="departamento_id"/>
                        </group>
                        <group>
                            <field name="sample_identifier" required="1"/>
                            <field name="sample_type"/>
                            <field name="date_received" readonly="1"/>
                            <field name="state" readonly="1"/>
                        </group>
                        <group>
                            <field name="custody_chain_id"/>
                        </group>
                        <group>
                            <field name="attachment_ids" widget="many2many_binary"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Vista de Lista para Recepción de Muestras -->
        <record id="view_lims_sample_list" model="ir.ui.view">
            <field name="name">lims.sample.list</field>
            <field name="model">lims.sample</field>
            <field name="arch" type="xml">
                <list>
                    <field name="cliente_id"/>
                    <field name="sucursal_id"/>
                    <field name="departamento_id"/>
                    <field name="sample_identifier"/>
                    <field name="sample_type"/>
                    <field name="date_received"/>
                    <field name="state"/>
                </list>
            </field>
        </record>

        <!-- Acción para abrir la vista de Recepción de Muestras -->
        <record id="action_lims_samples" model="ir.actions.act_window">
            <field name="name">Recepción de Muestras</field>
            <field name="res_model">lims.sample</field>
            <field name="view_mode">list,form</field>
        </record>

        <!-- Menú para Recepción de Muestras -->
        <menuitem id="lims_menu" name="LIMS" sequence="1" groups="base.group_user"/>
        <menuitem id="lims_reception_menu" name="Recepción de Muestras" parent="lims_menu" action="action_lims_samples" sequence="2"/>
    </data>
</odoo>
