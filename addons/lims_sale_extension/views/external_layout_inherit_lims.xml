<odoo>
  <template id="external_layout_inherit_lims"
            inherit_id="web.external_layout_standard">
    <!-- 1) Reducir el logo -->
    <xpath expr="//div[contains(@class,'d-flex')]/img" position="attributes">
      <attribute name="class">o_company_logo_small</attribute>
      <attribute name="style">max-height:80px; margin-bottom:0.5em;</attribute>
    </xpath>

    <!-- 2) Ajustar fuente y espaciado de la dirección -->
    <xpath expr="//div[@name='company_address']/ul" position="attributes">
      <attribute name="style">font-size:9px !important; line-height:1.2;</attribute>
    </xpath>

    <!-- 3) (Opcional) Ocultar país u otros textos fijos -->
    <!--
    <xpath expr="//li[t[contains(., 'Tax ID')]]" position="replace">
      <li style="display:none;"/>
    </xpath>
    -->

    <!-- 4) Ajustar pie de página: tamaño de fuente y espaciado -->
    <xpath expr="//div[contains(@class,'o_footer_content')]" position="replace">
      <div class="o_footer_content" style="font-size:8px; padding-top:2px;">
        <table style="width:100%; table-layout:fixed;">
          <tr>
            <!-- Fila 1: Contacto -->
            <td style="text-align:left;"><t t-raw="company.report_footer"/></td>
          </tr>
          <tr>
            <!-- Fila 2: Valor RFC u otra leyenda -->
            <td style="text-align:left;"><t t-if="company.vat">RFC: <t t-esc="company.vat"/></t></td>
          </tr>
          <tr>
            <!-- Fila 3: Paginación -->
            <td style="text-align:left;">
              <div t-if="report_type=='pdf'">Página <span class="page"/> / <span class="topage"/></div>
            </td>
          </tr>
        </table>
      </div>
    </xpath>

  </template>
</odoo>
